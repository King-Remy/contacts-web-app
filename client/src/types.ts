export interface Contact {
    contact_id: string;
    firstName: string;
    lastName: string;
    phoneNumber: string;
    address: string
}

export interface RawContact {
    id: string;
    first_name: string;
    last_name: string;
    phone_number: string;
    address: string;
  }